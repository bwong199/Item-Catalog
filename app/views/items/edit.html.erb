<%= form_for(@item, url: item_path(@item), html: {method: "patch"}) do |f| %> 
<%=	f.label :name, "Edit Item" %> 
<%= f.text_field :name, class: 'form-control' %> 

<br>
<%= f.submit "Edit", class: "btn btn-primary" %> 
<% end %> 